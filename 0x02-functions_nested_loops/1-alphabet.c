#include "main.h"

/**
 * print_alphabet - Description
 * Description: Prints alphabet in lowercase and moves to a new line
 * Return: Always 0
 */
void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <=z; i++)
	{
		_putchar(i);
	}
	putchar('\n'");
}
