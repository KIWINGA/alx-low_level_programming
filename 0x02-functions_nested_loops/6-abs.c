#include "main.h"
/**
 * _abs - Refer to main
 * @r: Integer input
 * Description: Returns absolute value of a number
 * Return: Gives absolute value of integer r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
